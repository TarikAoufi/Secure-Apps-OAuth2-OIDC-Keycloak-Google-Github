import { Injectable } from '@angular/core';
import { Observable, Subject } from 'rxjs';
/*
@Injectable({
  providedIn: 'root'
})*/
export class ConfirmationModalService {
/*
  private confirmationSubject = new Subject<void>();
  private closeModalSubject = new Subject<void>();

  constructor() { }

  getConfirmation(): Observable<void> {
    return this.confirmationSubject.asObservable();
  }

  confirm(): void {
    this.confirmationSubject.next();
  }

  getCloseModalSignal(): Observable<void> {
    return this.closeModalSubject.asObservable();
  }

  closeModal(): void {
    this.closeModalSubject.next();
  }
  */
}
